<div class="row-fluid offersnav">
  <div class="span12">

    <div class="span2 link<%= " active" if params[:action] == 'show' %>">
      <%= link_to "Νέες", @vendor %>
    </div>

    <div class="span3 link<%= " active" if params[:action] == 'expiring' %>">
      <%= link_to "Λήγουν σύντομα", expiring_vendor_path(@vendor) %>
    </div>

    <div class="span2 link<%= " active" if params[:action] == 'popular' %>">
      <%= link_to "Δημοφιλείς", '#' %>
    </div>

    <div class="span2 link<%= " active" if params[:action] == 'popular' %>">
      <%= link_to "1+1", '#' %>
    </div>

  </div>
</div>
