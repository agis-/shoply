<% content_for :sidebar, 'shared/categories_menu' %>
<% provide :title, @vendor.name %>

<div class="row-fluid">
  <div class="span12">
    <%= render 'broadcast' unless @vendor.broadcast.nil? %>
  </div>
</div>

<%= render 'sortby_links' if params[:c].nil? %>

<%= render partial: 'offers/offers', object: @offers %>